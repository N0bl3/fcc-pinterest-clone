version: "2"
services:
  app:
    build: .
    image: thinterest:latest
    container_name: thinterest_latest
    env_file: .env
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: development
      MONGODB_URI: "mongodb://mongo:27017"
    links:
      - mongo
    command: ["node", "server.js"]
  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
